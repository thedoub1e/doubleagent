{
  "name": "doubleagent",
  "version": "0.0.1",
  "private": true,
  "description": "线条小狗 AI 桌宠 — 送给留学伴侣的数字陪伴者 (macOS)",
  "author": "thedoub1e",
  "license": "MIT",
  "main": "./out/main/index.js",
  "scripts": {
    "dev": "electron-vite dev",
    "start": "electron-vite build && electron-vite preview",
    "build": "electron-vite build",
    "typecheck": "tsc --noEmit -p tsconfig.typecheck.json",
    "test": "vitest run",
    "test:e2e": "electron-vite build && node test/e2e/sessions.e2e.mjs"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "electron": "^34.2.0",
    "electron-vite": "^5.0.0",
    "playwright": "^1.60.0",
    "typescript": "^5.7.2",
    "vite": "^6.0.7",
    "vitest": "^4.1.8"
  },
  "dependencies": {
    "@earendil-works/pi-ai": "^0.79.1"
  }
}
