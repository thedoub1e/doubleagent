# Errors — 错题本（踩坑 + 解法）

> 记录每个错误、根因、解法，避免重复踩坑。只增不改。
> 格式： [YYYY-MM-DD HH:MM] 现象 → 根因 → 解法
> Created: 2026-06-12

## 预判坑位（实现阶段逐一验证 / 补充解法）
- 对方没装 Node → 安装.command 先装 Homebrew 再 brew install node，并检测版本。
- 网络慢 / npm 装不动 → 教程提供 npm 国内镜像（npmmirror）切换说明。
- Gatekeeper「无法验证开发者」→ 用「从源码 npm start 运行」而非打包 .app 规避；若仍弹窗，教程给「右键→打开」或 xattr -dr com.apple.quarantine 兜底图文。
- Apple Silicon vs Intel 架构 → Electron/Node 用对应架构；脚本里 uname -m 检测。
- API Key 泄漏进 git → .gitignore 屏蔽 .env/本地配置；仓库只放 .env.example。

---

## 实战踩坑
- [2026-06-12 15:13] npm install 报 ERESOLVE：现象——root 装 vite@^6，但 electron-vite@2.3.0 的 peer 只认 vite ^4||^5，依赖树解析失败。根因——pin 了过旧的 electron-vite。解法——electron-vite 最新稳定版 5.0.0 已支持 vite ^5||^6||^7，package.json 改 electron-vite:^5.0.0 后重装。教训：脚手架定版前先 npm view <pkg> peerDependencies 核对 peer 区间。
- [2026-06-12 15:40] 桌宠点不动也拖不动：现象——狗显示正常但点击/拖动无反应。根因——窗口启动即 setIgnoreMouseEvents(true,{forward:true})，依赖渲染层 mouseenter 翻转为可交互；但置顶+skipTaskbar 无焦点窗口的鼠标转发不可靠，mouseenter 从未触发→窗口永久忽略鼠标→点击穿透到桌面、-webkit-app-region 拖动同时失效（单一根因双症状）。解法——改为：窗口默认可交互(ignore=false)；弃用 app-region 改手动拖动(mousedown 记 screenX/Y→IPC dragBy 增量移动窗口)；点击穿透改用 document mousemove + elementFromPoint 判断指针是否在 .pet 上，只在落到透明背景时才 ignore(forward:true)，拖动中强制不 ignore。
- [2026-06-12 15:55] 点击切表情无反应：现象——能拖动但点气泡按钮表情不变。根因——setMood 用 SVG <g>.innerHTML=FACE 注入子节点，innerHTML 在 SVG 元素上解析出的 circle/path 落入 HTML 命名空间不渲染（初始脸因走整段<svg>经HTML解析器故正常）。解法——setMood 改为重建整段 stage.innerHTML=dogSvg(FACE[mood])，走 HTML 解析器路径保证 SVG 命名空间正确。
- [2026-06-12 16:20] MiniMax 真实调用 401 authentication_error(invalid...)：现象——pi-ai getModel(minimax,MiniMax-M3) 走 api.minimax.io/anthropic,传 {apiKey} 调用返回 401 鉴权失败(key 是 sk-cp- Coding Plan,125 长度,确认已读到)。疑因——pi-ai 的 anthropic-messages 默认按标准 Anthropic 发 x-api-key 头,但 MiniMax anthropic 兼容端点要 Authorization: Bearer(Claude Code 的 ANTHROPIC_AUTH_TOKEN 模式)。待查官方接法验证+修头。
- [2026-06-12 16:35] MiniMax 401 解决：curl 直打确认——不是鉴权头问题(Bearer/x-api-key 都行)，是站点错了。key 是 MiniMax 国内站(api.minimaxi.com,多个i)申请的,国际站 api.minimax.io 一律 401 invalid api key。pi-ai 的 complete/stream 不认 options.baseUrl 覆盖,须切 provider。解法——config 默认 provider 由 minimax 改 minimax-cn(端点 api.minimaxi.com/anthropic,同样有 M3/M2.7/highspeed)。实测 minimax-cn 流式+非流式均 200,陪伴人设回复正常。注:国内站 key,伴侣在国外需确认 api.minimaxi.com 可达(公网 API 通常可入)。
- [2026-06-13 15:20] 工具调用冒烟测试 ByteString 报错：现象——临时 smoke 脚本调 MiniMax 一律报 "Cannot convert argument to a ByteString...index 13 value 65288(（)"，与是否带 tools/中英文无关。根因——脚本用整文件正则 /MINIMAX_API_KEY=.../(无锚点)取 key，误匹配到 .env 里一行被注释的占位示例「…（你的真实Key）…」，全角括号进了 X-Api-Key 头。非 app bug：app 的 env.ts 逐行解析会跳过 # 注释行，用另一行真实 125 字符 key。解法——smoke 改用 app 同款逐行解析(跳注释+去引号)后工具调用一次通过(模型吐 create_reminder + 相对日期 明天→2026-06-14 正确)。教训：解析 .env 必须逐行跳注释，别用贪婪整文件正则。
- [2026-06-13 16:41] Open-Meteo 地理编码 language=zh 把英文城市名错配 → 现象：查 "New York" 用 language=zh 返回「约克(内布拉斯加)」lat40.86/lon-97.59，而非纽约市；但 language=en 查中文「北京」又返回无 results。根因：geocoding 的 language 参数同时影响本地化显示名与候选排序，跨脚本输入会错排/失配。解法：weather.ts geocodeLanguage(city) 按输入是否含中日韩字选 zh/en（含 CJK→zh，否则→en），buildGeocodeUrl 据此拼 language；实机验证 New York(en)→纽约、北京(zh)→北京 均正确。加单测锁住。
- [2026-06-13 16:57] dev 启动崩溃 ERR_PACKAGE_PATH_NOT_EXPORTED (@earendil-works/pi-ai) → 现象：npm run dev，electron app 加载即抛「No "exports" main defined in pi-ai/package.json」白屏不启动。根因：chat.ts 顶部 `import { Type } from '@earendil-works/pi-ai'` 是静态值导入；pi-ai 的 package.json exports 只给了 ESM "import" 条件、无 "require"，而 electron-vite 把 node_modules 依赖 externalize、主进程是 CJS → 静态导入编译成 require() 解析失败。其余调用都用动态 import() 规避了，唯独上个 session 加 createReminderTool 时引入的 `Type`(TypeBox,来自 typebox 再导出) 是静态值导入，且那之后没在 dev 实跑过故未暴露。解法：删掉该静态导入，工具 parameters 改为手写「纯 JSON Schema 对象」(TypeBox Type.Object 运行时本就序列化成等价 JSON Schema,发给模型一致)，加 defineTool() 帮手 + `as unknown as Tool` 转型；`import type { Tool, ToolCall }` 是纯类型导入、编译期擦除无运行时依赖故保留。修后 dev 正常启动、108测/typecheck 全过。教训：静态 import 任何 ESM-only 包到 CJS 主进程都会炸，新增 pi-ai 相关一律走动态 import 或纯类型导入。
- [2026-06-14 17:30] 聊天回复显示红色「[object Object]」(用户截图,在纠正「不是花生是海鲜」时触发) → 根因：chat.ts runChat 里 `handlers.onError(String(event.error))`，pi-ai 的 error 事件 error 字段是**对象**时 `String(对象)`="[object Object]"，把真实错误吞了 → 解法：加 errToText(err) 安全提取(string 原样/Error 取 .message/对象取 message·error 字段/兜底 JSON.stringify)，runChat 两处 onError 都改用它；渲染层 onError 也加防御(非字符串取 .message 或兜底文案)。真实错误信息今后会显示出来，便于定位底层 API 是否真出错(可能为一次性网络/限流)。
- [2026-06-14 17:50] 修[object Object]后露出真错「assistantMsg.content.flatMap is not a function」(用户纠正第二轮触发) → 根因：runChat 把历史消息 content 存成**字符串**，但 pi-ai transform-messages.js:68 对 assistant 历史消息做 `assistantMsg.content.flatMap(block=>..)`，字符串无 flatMap→崩。第1轮上下文无历史助手消息正常，第2轮带上一条助手回复就炸。**live 场景测试一直没catch=它只发单条 user 消息从无历史助手消息**。解法：runChat 里 history→messages 的 content 改成内容块数组 `[{type:'text',text:m.content}]`(pi-ai 的 canonical 形状)，图片块追加进该数组；scenario.live 加 runHistory + 2 个多轮回归用例(带历史助手消息的第二轮/多轮+工具)。真模型验证：全 12 live 用例过。教训：多轮(带历史助手消息)是必测路径，单轮测试会漏掉 content 形状 bug。
- [2026-06-14 19:20] Phase1/2 安全测试抓到两个真实坑：①macOS `/var`→`/private/var`、`/tmp`→`/private/tmp` 是符号链接，write_file 的 realpath 防越界校验把临时目录里的合法写入也拦了(realParent=/private/var… 不 startsWith root=/var…) → 解法：对 ctx.roots 也 realpath 后再比对(realRoots)。②Node `new URL('http://[::ffff:127.0.0.1]')` 把 IPv4 映射 IPv6 规范化成十六进制 `::ffff:7f00:1`，SSRF 正则按点分十进制匹配不上 → 解法：去掉 hostname 方括号后，对 `::ffff:` 映射一律拦(少见高风险)，并覆盖 ::1/fe80/fc00/fd。教训：沙箱/SSRF 这类安全闸必须用真实路径(realpath)和真实 URL 规范化结果来测，凭直觉写正则会漏。
- [2026-06-14 20:50] 用户人工验收发现3问题(电脑能力上线后):①危险操作确认卡片弹出时，聊天窗因失焦自动隐藏，用户得手动重开 → 根因 blur→hide 对确认场景太激进 → 解法：blur 守卫 pendingConfirms.size>0 时不隐藏 + requestConfirm 先 set 再 showChat。②确认卡片后的回答出现在卡片"上方"(错位) → 根因 send() 预建的流式气泡在卡片之前，跨轮全文又灌进它 → 解法：onConfirm 时整条移除当前气泡，让全文于卡片下方新气泡渲染(onDone 加兜底补气泡)。③**模型编造文件内容**：问"读 ~/.ssh/id_rsa"，read_file 实际已按 isSensitivePath 拒绝，但模型凭空编出"-----BEGIN OPENSSH PRIVATE KEY----- 私钥内容"假装读到了 → 根因 LLM 行为(嘴上答应/凭知识编造)，不是代码漏读 → 解法：人设加「绝不编造铁律」(文件/命令事实必须用工具拿真实结果，工具拒绝就如实说，绝不假装看过)。全量能力测试(capability.live)验证：拒读密钥零泄露+读文件不编造。教训：给 agent 接真实能力后，必须测「模型不调工具/工具拒绝时会不会编造」，这是信任底线。
- [2026-06-15 09:40] 用户报"查马德里奶茶店→网没连上(超时)" → 双重根因：①Node 的 fetch(undici) 默认 Happy-Eyeballs 先试 IPv6，该机器有死的 IPv6 路由→挂起到超时(curl 走 IPv4 就通)。解法：index.ts 启动 `setDefaultResultOrder('ipv4first')`，百度/Bing 从超时变 159ms/1s。②只有 fetch_url(开指定网址)、无开放式搜索，模型只能瞎猜 URL→超时。解法：加 web_search 工具。选后端踩坑：DuckDuckGo(html/lite)+Google 从此网络 Node fetch 仍超时(curl 通 fetch 不通，疑 IPv6/限流)，Bing 稳定 200；Bing 标记常改版(<li class="b_algo"> 在、<h2> 没了)→用宽容解析(每块抓首个 https 链接+strip 可见文字)抗改版。教训：①给 Electron/Node 接联网能力先设 ipv4first；②免费搜索爬取脆弱，Bing+宽容解析最稳，长期应考虑官方 API(含 Google Places)。
- [2026-06-15 14:42] 自更新功能上线后 dev 启动日志报 `Unable to set login item: 未能完成该操作。Operation not permitted`(platform_util_mac) → 根因：whenReady 里每次启动都 `applyLoginItem(loadConfig().autoLaunch)`，即便 autoLaunch=false 也调 setLoginItemSettings({openAtLogin:false,path,args})；dev 环境(非打包、从终端跑的 electron)设置登录项被系统拒，native 层直接打 ERROR 日志(JS try/catch 拦不住 native 日志)。虽不致命(只是想确保"关")但噪音且暴露"dev 下登录项操作可能被拒"。解法：启动同步改成「仅当 autoLaunch=true 才 applyLoginItem(true)」，关闭态不每次触碰系统设置；关闭动作仍由 config:set 里 applyLoginItem(false) 触发一次。教训：setLoginItemSettings 的失败是 native 日志、JS 不抛，别靠 try/catch 消音——靠"没必要就不调"。真机(她登录拉起)仍是待验项。
- [2026-06-15 18:20] 用户问"你自己按验收清单跑过吗" → 我跑了 gated 真模型测试(SCENARIO_LIVE=1),暴露并修了两个问题：①**test/ 从来没进 typecheck**(tsconfig include 只有 src)→ 我这程给 AppConfig 加的新字段(autoLaunch/dataVersion/autoCheckUpdate)在 scenario.live/capability.live 的 baseConfig 字面量里缺失却没报错，"typecheck 全绿"是假绿(漏扫测试)。解法：建 tsconfig.typecheck.json(extends 主配置 + include test)，typecheck 脚本切过去；补全两个 baseConfig 字段；修出 3 个早存在的未使用变量(capability vi/r、presence PresenceState)。今后 test 类型错会被抓。教训：typecheck 必须覆盖 test/，否则测试里的类型漂移(尤其跟着 src 类型变的 mock/fixture)会静默累积。②**capability.live「写文件」用例 ~50% flaky**：5 次跑 3 挂 2 过，模型(MiniMax-M3)对"新建 hello.txt"有时不调 write_file/run_command(直接口头答应或把内容inline)，confirmCalls=0 → 断言挂。**非回归**(PERSONA/工具接线本程没动；安全关键项拒密钥/沙箱/危险确认/不编造全稳过；新 set_briefing 路由稳过)。属 LLM 工具调用固有非确定性，测试已 gated。未加重试以免掩盖真回归。
- [2026-06-15 18:32] 补正上一条 live flaky 结论:多跑几轮发现失败用例在「工具触发类」之间**轮换**——run1 写文件挂、run2 写文件过/start_focus挂、run3 run_command挂。即不是某一条特定坏，而是 MiniMax-M3 对"该不该调动作工具(write/run/start_focus)"有约 ~10%/条的非确定性，suite 里十几条工具用例总会随机抖 1 条。**安全关键断言(拒密钥/沙箱越界/不编造/危险确认)跨多轮全稳**。结论:offline(250)+E2E(23)+typecheck 是真闸门、全绿；live 是诊断性(gated 不进 CI 闸)，单条失败先单跑复验，连挂多次且固定同一条才视作回归。未加自动重试以免掩盖真回归(若要降噪可给工具触发断言加 retry-once，但本项目已 gated 故暂不动)。
- [2026-06-16 多桌面 Space 跳转] 用户报「在桌面二点对话框，被强制带回桌面一回答」。先查证非本程引入：showChat 的 .show()+.focus() 与窗口 setVisibleOnAllWorkspaces 最后一次改动是老 commit 9cd5573(主动监督)，我近几程(自更新/自启动/小优化/撤Maps)没碰窗口显示/聚焦/跨桌面逻辑 → 非回归，是既有架构缺陷。根因：app 没设 accessory，是普通 app→「拥有」启动时那个 Space(桌面一)；聊天窗 .focus() 激活整个 app→macOS 把用户拽回 app 老家桌面，盖过了窗口的 visibleOnAllWorkspaces。解法：①whenReady 里 `app.setActivationPolicy('accessory')`(macOS)——桌宠改为桌面代理，不占 Dock、不拥有 Space；②抽 presentChatWindow() helper，show 前重申 setVisibleOnAllWorkspaces(true,{visibleOnFullScreen:true})，toggleChat/showChat 复用。验证：typecheck+E2E23(真启App，accessory 不破坏窗口/交互)过；**多桌面切 Space 行为 E2E 测不了，需真机双桌面验证**。副作用：accessory 下 Dock 不再显示该 app 图标(桌宠应有的形态，可接受)。

## [2026-06-16] 多桌面 Space 跳转：accessory+visibleOnAllWorkspaces 不足，真因是 focus() 二次激活
- 现象：桌面二点小狗，聊天窗弹出但用户被拽回桌面一(app 启动所在桌面)。50a3f5f 已尝试 app.setActivationPolicy('accessory')+显示前重申 setVisibleOnAllWorkspaces，但未真机验证，实测仍跳。
- 根因：presentChatWindow 用 chatWindow.show()+focus()。Electron 在 macOS 上 win.focus() 会调 [NSApp activateIgnoringOtherApps:YES] 二次激活 app→macOS 切到 app 的「归属 Space」(launch space)。而用户点小狗的物理点击本已把 app 在当前桌面就地激活，再 focus() 纯属重复激活且触发跨 Space 跳。
- 解决：改用 chatWindow.showInactive()(显示但不激活 app→不切 Space) + moveTop()(置顶,窗口 alwaysOnTop floating) + webContents.focus()(app 已被点击激活，键盘输入仍进输入框)。窗口创建时即 canJoinAllSpaces，本就显示在所有桌面/当前桌面，无需切 Space。renderer 加 visibilitychange→visible 时 inputEl.focus()(showInactive 不自动聚焦 DOM 输入框)。
- 教训：window.focus()=app 激活=可能跨 Space 跳；菜单栏/桌宠类「就地弹出」窗口应避免 show()/focus()，用 showInactive()+webContents.focus()。仅真机双桌面可终验，自动化测不了 Space 切换。

## [2026-06-18] 上线前总检查 · code-reviewer 审出 3 个 bug(0 CRITICAL/0 HIGH)
1. [MEDIUM] 打字机 leadingTagPending 误伤:正文以 `[` 开头且该行 `]` 来得晚的回复(如 "[1. 先去…")会被当成「未传完的情绪标签」暂缓吐字→空气泡直到 `]` 到达。非死循环(onDone 兜底写全文),但有空屏延迟。
   解决:leadingTagPending 加长度上限——仅当 `开头[ 且无] 且 t.length<=7`(情绪标签最多 [+6字]) 才暂缓,否则立即放行。renderer chat.ts。
2. [LOW] install.command 在 electron 还没起来就打印"✅已出现在桌面",用户可能以为失败而重跑→开两个实例。解决:文案改"正在启动,过几秒出现(第一次稍慢)"。
3. [LOW] .command 若经浏览器下载会丢执行位→双击 Permission denied(git clone 不丢,故影响小)。解决:脚本顶部自愈 `chmod +x "$0" 2>/dev/null || true`。
另:清理本会话临时诊断探头(diag()写/tmp,跳桌面排查用,showInactive 修复已留;ship 前移除避免给对象的版本带 /tmp 写入噪声)。补 LICENSE(MIT,package.json 已声明)。
