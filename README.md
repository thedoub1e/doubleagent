# 🐶 线条小狗 · 桌面 AI 陪伴

一只常驻在 macOS 桌面上的线条小狗，点一下就能像 Gemini 那样聊天。
它会**陪伴**你、温柔**监督**你的学习作息、随时**聊天**和**解惑** —— 给在外求学的你一点陪伴。

> 仅 macOS。模型用国产 **MiniMax**（默认），也支持 DeepSeek / 智谱GLM / Kimi / 通义 / Gemini 反代。
> API Key 只存在你自己电脑本地，**绝不上传、绝不进 git**。

<!-- 截图占位：在真机截几张图放进 docs/screenshots/ 后，把下面这段注释解开即可。
## 看看长什么样

| 桌面上的小狗 | 聊天 | 帮你做事 |
|---|---|---|
| ![桌宠](docs/screenshots/pet.png) | ![聊天](docs/screenshots/chat.png) | ![做事](docs/screenshots/agent.png) |
-->

---

## 一、安装（三步，不用碰命令行）

1. **拿到代码**：用 **GitHub Desktop**（推荐，最省心）把这个仓库 clone 到本地；
   或在仓库页点绿色 `Code` → `Download ZIP` 下载后解压。
2. **双击 `安装.command`**：
   - 如果电脑没装 Node.js，它会自动打开下载页，下载并安装 **LTS 版本**（一路点继续），
     装好后**再双击一次**`安装.command`。
   - 看到 `✅ 安装完成` 就好了。
3. **双击 `启动小狗.command`**：桌面右下角就会出现小狗 🐶。

> 💡 第一次双击 `.command` 如果提示「无法验证开发者 / 已阻止」，
> **右键点它 → 选「打开」→ 再点「打开」** 即可（只需这一次）。

---

## 二、填 API Key（第一次用）

小狗要连模型才能说话，需要一个 **MiniMax** 的 Key：

1. 打开 [platform.minimaxi.com](https://platform.minimaxi.com)（**国内站**）注册登录。
2. 在「账户管理 / 接口密钥」里**创建一个 API Key**，复制下来。
3. 在小狗聊天窗右上角点 **⚙ 设置** → 把 Key 粘进 **API Key** 框 → **保存**。

填好后，点小狗、打字、回车，它就会回复你啦。

> 默认模型源是 **MiniMax · 国内**、模型 **MiniMax-M3**。在国外如果连不上，
> 可在设置里换成 **MiniMax · 国际**（需用国际站 minimax.io 的 Key）。

---

## 三、怎么玩

- **点小狗** → 打开 / 收起聊天窗
- **按住小狗拖** → 把它挪到喜欢的位置
- **聊天窗 ⚙ 设置** 里可以：
  - 换**模型源 / 模型**（还有更便宜的「记忆模型」专做后台记笔记）
  - **填 Google Maps 密钥**（选填）→ 解锁「附近好吃好玩」推荐
  - 勾**开机自动启动小狗** → 一开机它就在桌面陪你
  - **换形象**：选一张你喜欢的图片 / GIF 当小狗（支持线条小狗素材）
  - **清空对话** / 看看并编辑「小狗眼中的你」（它记住你的事，透明可改）

> 主动监督、提醒时间、所在城市这些**不用进设置填表**——
> 直接跟小狗说就行（见下方「跟它说说看」）。

---

## 四、它能帮你做的事（直接说话就行，不用学命令）

小狗不只是会聊天，它能**真的在你电脑上动手**，也会**主动关心**你。随口说，它就懂：

**📝 记事 & 提醒**
- 「下周三早上交论文」「明天三点面试」 → 帮你记进 macOS 提醒事项，到点自动响
- 「每天九点提醒我背单词」「别在 23:30 喊我了」 → 每日定点提醒，随时增删
- 「期末考是 6 月 20」「回国倒计时」 → 帮你数着日子，早安时报进度
- 「essay 交了」 → 帮你把待办勾掉

**🌤️ 生活帮手**
- 「今天要带伞吗」「冷不冷」 → 查你所在地天气
- 「附近有什么好吃的」「想喝奶茶」 → 按你位置推荐（需填 Google Maps 密钥）
- 「在家不知道吃什么」 → 结合它了解的你的口味，推家常菜 + 找菜谱
- 「我搬到马德里了」 → 它记住，天气和推荐都跟着走

**💻 电脑上的事（你是电脑小白，交给它）**
- 「帮我看看这个文件写了啥」「这个文件夹里有什么」 → 帮你查看
- 「这个报错怎么解决」「查一下 XX」 → 帮你上网搜
- 「帮我清下缓存」「装个 XX」 → 帮你跑命令（**动手改电脑前会先弹确认给你点头**，危险操作自动拦）

**🍅 陪学 & 陪伴**
- 「陪我专注 25 分钟」「学到下午三点」 → 开番茄钟陪学、打卡
- 早安 / 晚安**简报**：早上报今天的待办和天气，睡前问问今天过得怎样（时间可让它改）
- 久坐太久会提醒你起来走走，久别重逢会跟你打招呼
- 心情不好时，它会先**好好听你说**，陪着你，而不是急着讲道理

**🔒 安全**：所有「动手改电脑」的操作都要你**点确认**才执行；只读的查看不打扰你；
危险命令（删库、关机等）一律自动拦下。它绝不会偷偷动你的东西。

---

## 五、常见问题（FAQ）

**Q：双击 `.command` 弹「无法验证开发者」？**
A：右键点它 → 「打开」→ 「打开」。只需第一次这样。

**Q：安装卡在下载、很久不动？**
A：多半是网络。安装脚本已自动用国内镜像；连好网后再双击一次 `安装.command`。

**Q：聊天报红色「401 / invalid api key」？**
A：Key 站点不对。**国内站(minimaxi.com)的 Key** 要配 **MiniMax · 国内**；
国际站(minimax.io)的 Key 配 **MiniMax · 国际**。在设置里对应切换。

**Q：小狗挡住东西了？**
A：透明区域是可以点穿的；按住小狗本体拖动就能挪走它。

**Q：怎么让它别一直开着？**
A：关掉那个黑色终端窗口，小狗就下班了；想它回来再双击 `启动小狗.command`。

---

## 隐私与安全

- API Key 只写在本地（应用数据目录 / 项目 `.env`），**不进 git、不上传任何服务器**。
- 没有云端后端、没有账号系统、没有遥测：你的聊天只在你电脑和你选的模型厂商之间。
